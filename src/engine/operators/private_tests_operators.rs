// This file will be replaced by the runner

mod advanced {

    /**
     * Tests a table scan, followed by a join (both small tables).
     */
    #[test]
    fn test_scan_and_join() {
        unimplemented!();
    }

    /**
     * Tests multiple filters on a large table.
     */
    #[test]
    fn test_large_filter() {
        unimplemented!();
    }

    /**
     * Tests multiple aggregations on a large table.
     */
    #[test]
    fn test_large_multi_agg() {
        unimplemented!();
    }

    /**
     * Tests a full query execution plan (filter,join,agg) on small tables.
     */
    #[test]
    fn test_query_plan_small_no_scan() {
        unimplemented!();
    }

    /**
     * Tests a full query execution plan (filter,join,agg) on large tables.
     */
    #[test]
    fn test_query_plan_large_no_scan() {
        unimplemented!();
    }
}
